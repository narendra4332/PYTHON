# tuple :- A tuple is a collection which is ordered and inmutable iterating through tuple is faster than with list

# 1.ordered data type 
# 2.()
# 3.inmutabl

# we can use this function in tuple...
# min()
# max()
# count()
# index()
# sum()
thistuple = (10,20,30,40,10)
print(thistuple)
# print(tuple(dict.fromkeys(thistuple)))
for element in range(len(thistuple)):
    print(thistuple[element])

# print("\n",min(thistuple))

# print("\n",max(thistuple))

# countnum = thistuple.count(10)
# print("\n",countnum)

# sum = sum(thistuple)
# print("\n",sum)

# newtpl=thistuple.index(20)
# print("\n",newtpl)

